const saySomethingz = (txt) => {
	console.log(txt)
}

const replyToAll = (text) => {}

const stopTalking = () => {
	const num = '1' + '2'
	console.log(num1)
}

const poopooz = (size) => {
	let dataToSend = ''

	switch (size) {
		case 'small':
			dataToSend = '💩'
			break
		case 'medium':
			dataToSend = '💩💩'
			break
		case 'large':
			dataToSend = '💩💩💩'
			break
		default:
			dataToSend = '💩'
			break
	}
	console.log(dataToSend)
}

const logMonkaS = () => {
	console.log('monkaS')
}
