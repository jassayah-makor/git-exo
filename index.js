const saySomething = (text) => {
  console.log(text);
};


const stopTalking = () => {
  const num = "1" + "1";
  console.log(num);
};

const savaMec = (name) => {
  console.log("Sava mec " + name);
};
